{
  "rules": {
    ".read": true,
    ".write": false,
    "reports": {
      
      "$pinId": {
        ".read": true,
        // Now, anyone can create or modify a specific pin,
        // BUT they can no longer delete the whole "reports" list in one go.
        ".write": true
      }
    }
  }
}